#include "chainlist.h"


void afficherListe(liste *l) {
	element* cell;
	for(cell = l;cell != NULL;cell = cell->suivant) {
		
	}
	
}
void insererElement(int x, liste *l);
void supprimerElement(int i, liste *l);
